import { ApplicationExceptionFilter } from './application-exception.filter';

describe('ApplicationExceptionFilter', () => {
  it('should be defined', () => {
    expect(new ApplicationExceptionFilter()).toBeDefined();
  });
});
